//BRONZE
var hobbies = ['reading', 'television', 'cuddles'];


//SILVER
var cars = ['Ferrari', 'GT', 'Mustang', 'Alpha Romeo'];

for (i = 0; i < cars.length; i++) {
	console.log(cars[i]);
}

//GOLD

var lists = [
	jobs= ["assistant", "clerk"],
	pets= ["fish", "dog"],
	cars= ["nissan", "hyundai"]
];
for (i = 0; i < lists.length; i++) {
	console.log(lists[i]);
}

//Other challenges
/*var randomNumbers = [Math.floor((Math.random()*50)+1),
	Math.floor((Math.random()*50)+1), Math.floor((Math.random()*50)+1),
	Math.floor((Math.random()*50)+1), Math.floor((Math.random()*50)+1),
	Math.floor((Math.random()*50)+1), Math.floor((Math.random()*50)+1),
	Math.floor((Math.random()*50)+1), Math.floor((Math.random()*50)+1),
	Math.floor((Math.random()*50)+1), Math.floor((Math.random()*50)+1),
	Math.floor((Math.random()*50)+1), Math.floor((Math.random()*50)+1),
	Math.floor((Math.random()*50)+1), Math.floor((Math.random()*50)+1)
	];
	console.log(randomNumbers);
	for () {
		if ( %  = 0)
	}*/